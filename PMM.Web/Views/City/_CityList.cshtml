
@model CityListModel
@using PMM.Web.Models;
@using PMM.Core;

<div class="table-responsive">
    <table class="table table-bordered table-hover">
        @if (Model.Cities.Count > 0)
        {
            <thead>
                <tr class="headings">
                    <th class="bold">SRNO</th>
                    <th class="bold">TITLE</th>
                    <th class="bold">Action</th>
                </tr>
            </thead>
            <tbody>
                @for (int i = 0; i < Model.Cities.Count; i++)
                {

                    var item = Model.Cities[i];

                    <tr class="pointer">

                        <td>@(i + 1)</td>
                        <td class="wordWrap">@item.Title</td>
                        <td>
                            <a href='@Url.RouteUrl("ManageCity", new { id = item.Id })' class="btn btn-info btn-xs"><i class="fa fa-pencil"></i> Edit </a>
                            <a href="javascript:DeleteCity(@item.Id);" class="btn btn-danger btn-xs"><i class="fa fa-trash-o"></i> Delete </a>

                        </td>

                    </tr>


                }
            </tbody>
        }
        else
        {
            <tr class="col-md-12 pl5 pr5">
                <td>
                    <span class="font14">@CommonHelper.ValMsgEmptyNoRecord</span>
                </td>
            </tr>
        }
    </table>

</div>